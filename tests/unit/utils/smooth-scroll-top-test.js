import smoothScrollTop from 'mobile-wiki/utils/smooth-scroll-top';
import { module, test } from 'qunit';

module('Unit | Utility | smoothScrollTop', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = smoothScrollTop();
    assert.ok(result);
  });
});
