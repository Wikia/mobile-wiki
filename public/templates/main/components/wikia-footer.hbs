{{svg "wikia-logo-white" role="img" class="logo"}}
<ul class="footer-links">
	{{#each links}}
		<li {{bind-attr class="spaced:spaced"}}>
			<a {{bind-attr href="href"}}>{{i18n text}}</a>
		</li>
	{{/each}}
</ul>
