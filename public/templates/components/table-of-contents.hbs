<span {{action 'toggleMenu'}} {{bind-attr class=":title isCollapsed:collapsed"}}>
	<svg viewBox="0 0 18 13" class="icon toc">
		<use xlink:href="#toc"></use>
	</svg>
	<svg viewBox="0 0 12 7" class="icon chevron">
		    <use xlink:href="#chevron"></use>
	</svg>
	{{yield}}
</span>
<ul {{bind-attr class="isCollapsed:collapsed"}}>
	{{#each sections}}
	<li><a href="#{{unbound id}}" alt="{{unbound name}}">{{unbound name}}</a></li>
	{{/each}}
</ul>
