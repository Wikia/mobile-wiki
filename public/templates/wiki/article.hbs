<section class="article-body">
	<h1 class="article-title">{{cleanTitle}}</h1>
	{{#collapsible-menu showMenuIcon=true additionalClasses="table-of-contents" label="Table of Contents"}}
		{{#each sections}}
		<li><a href="#{{unbound id}}" title="{{unbound name}}">{{unbound name}}</a></li>
		{{/each}}
	{{/collapsible-menu}}
	<article class="article-content">
		{{{article}}}
	</article>
</section>
<section class="article-footer">
	<ul class="contributors">
		{{#each users}}
		<li><a href="{{unbound url}}"><img src={{unbound avatar}} alt="{{unbound name}}"></a></li>
		{{/each}}
	</ul>
	{{#if comments}}
	<div class="comments">
		{{i18n T.load-n-comments count=comments}}
	</div>
	{{/if}}
</section>
{{content-recommendations pages=relatedPages}}
