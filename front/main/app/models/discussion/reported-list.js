import Ember from 'ember';

const DiscussionReportedList = Ember.object.extend({
	contributors: null,
	count: null,
	items: null,
	pageNum: null,
	pivotId: null,
});

export default DiscussionReportedList;
