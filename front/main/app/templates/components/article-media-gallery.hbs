{{#each itemsToRender as |item|}}
	{{article-media-image
		url=item.url
		ref=ref
		galleryRef=item.galleryRef
		itemContext='gallery'
		type=item.type
		forcedWidth=imageSize
		forcedHeight=imageSize
		cropMode=cropMode
	}}
{{/each}}
