{{#each items as |item|}}
	{{article-media-image
		url=item.url
		link=item.link
		galleryRef=item.galleryRef
		type=(concat 'gallery-' item.type)
		forceWidth=imageSize
		forceHeight=imageSize
		cropMode=cropMode
	}}
{{/each}}
