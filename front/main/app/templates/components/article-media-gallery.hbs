{{#each items as |item|}}
	{{article-media-image
		url=item.url
		link=item.link
		ref=ref
		galleryRef=item.galleryRef
		itemType=(concat 'gallery-' item.type)
		forceWidth=imageSize
		forceHeight=imageSize
		cropMode=cropMode
	}}
{{/each}}
