{{!
	This template is linked with of app/extensions/wikia/PortableInfobox/templates/PortableInfoboxItemData.mustache
	Changing this template requires modifying the other in app repo.
}}
<div class="pi-item pi-data pi-item-spacing pi-border-color {{activeClass}}" {{action 'itemClicked'}}>
{{#if label}}
	<h3 class="pi-data-label pi-secondary-font">{{label}}</h3>
{{/if}}
	<div class="pi-data-value pi-font">{{value}}</div>
</div>
