{{#each-in collections as |id data|}}
	<h3 id="{{id}}">{{id}}</h3>
	{{#if data.hasPrev}}
		<div class="category-navigation">
			<button class="category-navigation__button" {{action attrs.loadBatch id data.prevBatch }}>
					Load Previous
			</button>
		</div>
	{{/if}}
	<ul>
		{{#each data.items as |item|}}
			<li><a href="{{item.url}}">{{item.name}}</a></li>
		{{/each}}
	</ul>
	{{#if data.hasNext}}
		<div class="category-navigation">
			<button class="category-navigation__button" {{action attrs.loadBatch id data.nextBatch }}>
					Load More
			</button>
		</div>
	{{/if}}
{{/each-in}}
