{{!
	This component template and its JS logic part are linked with of app/extensions/wikia/PortableInfobox/templates/PortableInfoboxItemData.mustache
	Changing them template requires modifying the other in app repo.
}}
{{#if label}}
	<h3 class="pi-data-label pi-secondary-font">{{label}}</h3>
{{/if}}
<div class="pi-data-value pi-font">{{value}}</div>
