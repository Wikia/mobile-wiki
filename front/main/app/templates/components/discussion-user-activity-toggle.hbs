<ul class="user-activity-options">
	<li class="{{if options.posts.isActive 'active'}}">
		{{#link-to 'discussion.moderator.user-activity.posts'}}
			{{i18n 'user-activity.active-users-toggle-option' ns='discussion'}}
		{{/link-to}}
    </li>
    <li class="{{if options.reports.isActive 'active'}}">
		{{#link-to 'discussion.moderator.user-activity.reports'}}
			{{i18n 'user-activity.reports-toggle-option' ns='discussion'}}
		{{/link-to}}
    </li>
    <li class="{{if options.moderatorActions.isActive 'active'}}">
		{{#link-to 'discussion.forum'}}
			{{i18n 'user-activity.moderator-actions-toggle-option' ns='discussion'}}
		{{/link-to}}
    </li>
</ul>

<div class="wds-dropdown {{if isDropdownActive 'wds-is-active'}}">
    <div class="wds-dropdown__toggle" {{action 'toggleDropdown'}}>
		<span>{{i18n 'user-activity.days-count' numberOfDays=days ns='discussion'}}</span>
        <svg class="wds-icon wds-dropdown__toggle-chevron">
            <use xmlns:xlink="http://www.w3.org/1999/xlink" xlink:href="#wds-icons-dropdown"></use>
        </svg>
    </div>
    <div class="wds-dropdown__content">
        <ul class="wds-list">
            <li {{action 'setDays' 30}}>{{i18n 'user-activity.days-count' numberOfDays=30 ns='discussion'}}</li>
            <li {{action 'setDays' 90}}>{{i18n 'user-activity.days-count' numberOfDays=90 ns='discussion'}}</li>
        </ul>
    </div>
</div>
