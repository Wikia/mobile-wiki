import App from '../app';

App.DiscussionNotFoundErrorComponent = Ember.Component.extend({
	classNames: ['discussion-error', 'columns', 'large-6'],
});

export default App.DiscussionNotFoundErrorComponent;
