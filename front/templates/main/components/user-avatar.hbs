<figure class="avatar">
	<a href="{{profileUrl}}">
		<img src="{{avatarUrl}}">
	</a>
</figure>
{{#if displayUsername}}
<a href="{{profileUrl}}" class="avatar-username">
	{{#if truncateNameLength}}
		{{truncate username truncateNameLength}}
	{{else}}
		{{username}}
	{{/if}}
</a>
{{/if}}
