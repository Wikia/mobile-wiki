<div {{action 'hide'}} class="overlay"></div>
<ul class="drawer">
	{{#each link in links}}
		<li>
			<a href="{{unbound link.href}}" class="external">{{i18n 'app' link.textKey}}</a>
		</li>
	{{/each}}
</ul>
