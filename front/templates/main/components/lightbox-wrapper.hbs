{{#if type}}
	<div class="lightbox-header {{if headerHidden 'hidden'}}">
		<div class="lightbox-close-wrapper">
			<svg role="img" class="lightbox-close-button">
				<use xlink:href="#close"></use>
			</svg>
		</div>
		<div class="lightbox-header-title">{{header}}</div>
	</div>
	<div class="lightbox-content">
		{{component lightboxComponent
			model=model
			queryParamFile=queryParamFile
			queryParamMap=queryParamMap
			setHeader='setHeader'
			setFooter='setFooter'
		}}
	</div>
	{{#if footer}}
		<div class="lightbox-footer {{if footerExpanded 'expanded'}} {{if footerHidden 'hidden'}}">{{footer}}</div>
	{{/if}}
{{/if}}