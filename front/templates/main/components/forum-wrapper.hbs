{{discussion-editor
	active=editorActive
	forumId=forumId
	siteHeadPinned=siteHeadPinned
	setSortBy=setSortBy
	posts=posts
	createPost='createPost'
	shouldShowError=shouldShowPostError
}}
{{#each posts as |item|}}
	{{post-detail
		author=item.createdBy
		goToPost='goToPost'
		post=item._embedded.firstPost.[0]
		postId=item.id
		replyCount=item.postCount
		upvote='upvote'
		isVisible=item._embedded.firstPost.[0].isVisible
	}}
{{/each}}
{{#if hasMore}}
	{{#if isLoading }}
		{{loading-spinner
			active=true
			overlay=false
			radius=32
			strokeWidth=2
		}}
	{{/if}}
	<div class="load-more">
		{{i18n 'main.loading-more-posts' ns='discussion'}}
	</div>
{{/if}}
