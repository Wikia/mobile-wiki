<div class="category-members-grouped">
	{{#if model.prevPageUrl}}
		<div class="category-navigation">
			<a href="{{model.prevPageUrl}}" class="wds-button category-navigation__button">
				{{i18n 'app.category-page-load-previous'}}
			</a>
		</div>
	{{/if}}
	{{#each model.membersGrouped as |members firstLetter|}}
		<div class="category-members-group">
			<h3>{{firstLetter}}</h3>
			<ul class="mw-content">
				{{#each members as |category|}}
					<li>
						<a href="{{category.url}}">{{category.title}}</a>
					</li>
				{{/each}}
			</ul>
		</div>
	{{/each}}
	{{#if model.nextPageUrl}}
		<div class="category-navigation">
			<a href="{{model.nextPageUrl}}" class="wds-button category-navigation__button">
				{{i18n 'app.category-page-load-more'}}
			</a>
		</div>
	{{/if}}
</div>
