<nav class="collapsible-menu">
	<div class="collapsed title">
		{{i18n 'app.article-other-languages'}}
	</div>
	<ul>
		{{#each otherLanguages as |link|}}
			<li class="mw-content">
				<a href="{{link.url}}" title="{{link.title}}">
					{{link.languageName}} | {{link.articleTitle}}
				</a>
			</li>
		{{/each}}
	</ul>
</nav>
