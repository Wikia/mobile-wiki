{{#unless queryParams.noexternals}}
	<script>
		M.tracker.Internal.initialize();
		M.tracker.Internal.trackPageView({
			a: M.prop('pageTitle'),
			n: M.prop('mediaWikiNamespace')
		});
	</script>
{{/unless}}
