<script>
	(function() {
		function isTestCommunity () {
			// starwars, marvel, gameofthrones, fallout, elderscrolls
			var wikiId = Mercury.wiki.id,
				testCommunites = [
					147,
					2233,
					130814,
					3035,
					1706
				];

			return testCommunites.indexOf(wikiId) !== -1;
		}

		window.loadLiftigniter = function(w,d,s,p,v,e,r) {
			w['$igniter_var']=v;
			w[v]=w[v] || function(){
				(w[v].q=w[v].q||[]).push(arguments)
			};
			w[v].l=1*new Date();
			e=d.createElement(s),r=d.getElementsByTagName(s)[0];
			e.async=1;
			e.src=p+'?ts='+(+new Date()/3600000|0);
			r.parentNode.insertBefore(e,r)
		};

		window.trackLiftigniterPageView = function() {
			if (M.prop('queryParams.noexternals') || M.prop('environment') !== 'prod' || !isTestCommunity()) {
				return;
			}

			if (!window.$p) {
				window.loadLiftigniter(window,document,'script','//cdn.petametrics.com/l9ehhrb6mtv75bp2.js','$p');
				window.$p('init', 'l9ehhrb6mtv75bp2', {
					sdk: {
						multiFetch: true
					}
				});
			}

			window.$p('resetPageview');
		};

		window.trackLiftigniterPageView();
	})();
</script>
