import sponsoredContent from '@wikia/ember-fandom/services/sponsored-content';

// we have to extend it that way because Ember does not automatically use the one from addon
export default sponsoredContent.extend();
