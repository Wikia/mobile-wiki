{{! This wrapper is required so the chevron is positioned correctly }}
<div class="article-media-thumbnail-image-wrapper">
	{{#if isOgg}}
		<a href="{{model.href}}">{{svg 'wds-icons-download' viewBox='0 0 24 24' class='icon'}}</a>
	{{else}}
		<a href="{{model.href}}">
			<img alt="{{model.title}}"
				 src="{{thumbnailUrl}}"
				 height="{{if forcedHeight forcedHeight model.height}}"
				 width="{{if forcedWidth forcedWidth model.width}}">
			{{#if isLinkedByUser}}
				{{svg 'chevron-linked' viewBox='0 0 20 20' class='chevron'}}
			{{/if}}
		</a>
	{{/if}}

	{{#if isVideo}}
		<div class="article-media-thumbnail__play-icon">
			{{svg 'wds-player-icon-play' class='wds-player-icon-play-medium'}}
		</div>
	{{/if}}
</div>
{{! Allow custom captions }}
{{#if hasBlock}}
	{{yield}}
{{else}}
	{{#if hasFigcaption}}
		<figcaption>
			{{#if showTitle}}
				<div>{{model.title}}</div>
			{{/if}}
			{{{model.caption}}}
		</figcaption>
	{{/if}}
{{/if}}
