{{! This wrapper is required so the chevron is positioned correctly }}
<div class="article-media-thumbnail-image-wrapper">
	{{#if isOgg}}
		<a href="{{href}}">{{svg 'wds-icons-download' viewBox='0 0 24 24' class='icon'}}</a>
	{{else}}
		<a href="{{href}}">
			<img alt="{{title}}"
				 src="http://project43.sandbox-xw2.wikia.com/vignette/starter/images/a/a9/Example.jpg/revision/latest/fixed-aspect-ratio/width/412/height/375?cb=20060602123345"
				 height="{{if forcedHeight forcedHeight height}}"
				 width="{{if forcedWidth forcedWidth width}}">
			{{#if isLinkedByUser}}
				{{svg 'chevron-linked' viewBox='0 0 20 20' class='chevron'}}
			{{/if}}
		</a>
	{{/if}}

	{{#if isVideo}}
		<div class="article-media-thumbnail__play-icon">
			{{svg 'wds-player-icon-play' class='wds-player-icon-play-medium'}}
		</div>
	{{/if}}
</div>
{{! Allow custom captions }}
{{#if hasBlock}}
	{{yield}}
{{else}}
	{{#if hasFigcaption}}
		<figcaption>
			{{#if showTitle}}
				<div>{{title}}</div>
			{{/if}}
			{{{caption}}}
		</figcaption>
	{{/if}}
{{/if}}
