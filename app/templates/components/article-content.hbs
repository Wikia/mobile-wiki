{{#if html}}
	{{{html}}}
{{else if content}}
	{{#each content as |section|}}
		{{{section}}}
	{{/each}}
{{/if}}
{{#if isLoading}}
	{{wds-spinner
		active=true
		isBlock=true
		overlay=false
		radius=16
		strokeWidth=2
	}}
{{/if}}
