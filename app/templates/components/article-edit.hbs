<header class="sub-head">
	<a role="button" {{action back}} class="sub-head--cancel">{{svg 'wds-icons-arrow' role='img' class='wds-icon'}}</a>
	<button {{action publish}} class="sub-head--done">{{t 'edit.page-publish'}}</button>
	<div class="sub-head--title">{{t 'edit.page-heading'}}<span>{{model.title}}</span></div>
</header>
{{textarea value=model.content class='edit-textarea'}}
