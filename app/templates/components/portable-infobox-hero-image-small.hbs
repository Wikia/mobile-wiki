<div class="pi-item pi-data pi-item-spacing pi-border-color">
	{{!TODO i18n}}
	<h3 class="pi-data-label pi-secondary-font">Image</h3>
	<div class="pi-data-value pi-font">
		<figure>
			<a href="{{heroImage.url}}" {{action 'openLightbox' bubbles=false}}>
				<img class="article-media-placeholder"
						 src="{{imageSrc}}"
						 alt="{{heroImage.title}}">
			</a>
		</figure>
	</div>
</div>
