{{#if isEnabled}}
  {{#if bigUnit}}
    {{affiliate-unit
      heading=bigUnit.heading
      image=bigUnit.image
      link=bigUnit.link
      subheading=bigUnit.subheading
    }}
  {{else}}
    {{post-search-results
      query=query
      unit=smallUnit
      isCrossWiki=isCrossWiki
      debugAffiliateUnits=debugAffiliateUnits
      setHasAffiliateUnit=setHasAffiliateUnit
    }}
  {{/if}}
{{/if}}
