{{#if isEnabled}}
  {{#if bigUnit}}
    <AffiliateUnit @unit={{bigUnit}} />
  {{else}}
    <PostSearchResults @query={{query}} @unit={{smallUnit}} @isCrossWiki={{isCrossWiki}} @debugAffiliateUnits={{debugAffiliateUnits}} />
  {{/if}}
{{/if}}
