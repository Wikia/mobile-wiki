{{#if isEnabled}}
  {{#if bigUnit}}
    {{affiliate-unit
      unit=bigUnit
    }}
  {{else}}
    {{post-search-results
      query=query
      unit=smallUnit
      isCrossWiki=isCrossWiki
      debugAffiliateUnits=debugAffiliateUnits
    }}
  {{/if}}
{{/if}}
