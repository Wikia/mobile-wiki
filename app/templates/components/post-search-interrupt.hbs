{{#if this.isEnabled}}
  {{#if this.bigUnit}}
    <AffiliateUnit @unit={{this.bigUnit}} />
  {{else}}
    <PostSearchResults @query={{@query}} @unit={{this.smallUnit}} @isCrossWiki={{@isCrossWiki}} @debugAffiliateUnits={{@debugAffiliateUnits}} />
  {{/if}}
{{/if}}
