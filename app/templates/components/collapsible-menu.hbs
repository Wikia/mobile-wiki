<div role="button" {{action 'toggleMenu'}} class="{{if isCollapsed 'collapsed'}} title">
	<span>{{t tLabel count=tCount}}</span>
	{{svg 'wds-icons-menu-control-tiny' class='wds-icon wds-icon-tiny chevron'}}
</div>
{{#if ordered}}
	<ol>
		{{yield}}
	</ol>
{{else}}
	<ul>
		{{yield}}
	</ul>
{{/if}}
