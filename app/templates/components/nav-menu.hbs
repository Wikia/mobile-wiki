<ul class="nav-menu">
	{{#each items as |item|}}
		{{component item.type
			className=item.className
			name=item.name
			href=item.href
			route=item.route
			itemClick=(action onClickHandler item)
			changeItem=(action onChangeItemHandler item.index)
		}}
	{{/each}}
</ul>
