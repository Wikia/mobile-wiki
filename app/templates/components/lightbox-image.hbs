{{#if loadingError}}
	{{i18n 'app.media-lightbox-error'}}
{{else}}
	<img alt="{{model.title}}" class="current" src="{{imageSrc}}" style={{style}}>
{{/if}}
{{wds-spinner active=isLoading overlay=false}}
