{{#if this.loadingError}}
  {{i18n "app.media-lightbox-error"}}
{{else}}
  <img
    alt={{@model.title}}
    class="current {{if this.isLoading "loading"}}"
    src={{this.imageSrc}}
    style={{this.style}}
  >
{{/if}}
