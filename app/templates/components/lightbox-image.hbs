{{#if isLoaded}}
	<img alt="image placeholder" class="current" src="{{imageSrc}}" style={{style}}>
{{/if}}
{{#if loadingError}}
	{{i18n 'app.media-lightbox-error'}}
{{/if}}
{{wds-spinner active=isLoading overlay=false}}
