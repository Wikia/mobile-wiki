{{#if loadingError}}
  {{i18n "app.media-lightbox-error"}}
{{else}}
  {{! template-lint-disable no-inline-styles }}
  <img
    alt={{model.title}}
    class="current {{if isLoading "loading"}}"
    src={{imageSrc}}
    style={{style}}
  >
{{/if}}
