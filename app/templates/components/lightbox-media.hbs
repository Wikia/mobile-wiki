{{#if this.lightboxComponent}}
  {{component
    this.lightboxComponent
    model=this.currentMedia
    isGallery=this.isGallery
    header=this.header
    footer=this.footer
  }}
{{else}}
  {{i18n "app.media-lightbox-error"}}
{{/if}}
