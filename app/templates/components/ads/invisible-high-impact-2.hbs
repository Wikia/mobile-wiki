{{#unless noAds}}
	<div id="InvisibleHighImpactWrapper" class="wds-is-hidden invisible-high-impact-wrapper">
		<div class="background"></div>
		<div class="top-bar">
			<a class="close">
				{{svg 'close' role='img' class='icon close-button'}}
			</a>
			<div class="label">{{i18n 'app.advertisement-label'}}</div>
		</div>
		<div id="{{name}}" class="ad-slot wds-is-hidden {{nameLowerCase}}"></div>
	</div>
{{/unless}}
